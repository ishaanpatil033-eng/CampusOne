package com.campusone.config;

import org.springframework.context.annotation.Configuration;

/**
 * General application configuration.
 * Add bean definitions for shared utilities here as the app grows.
 */
@Configuration
public class AppConfig {
    // Placeholder for future beans (e.g., ModelMapper, ObjectMapper customization)
}
